import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  button: {
    width: "35%",
    padding: 12,
    alignItems: "center",
    borderWidth: 1,
    borderRadius: 12,
  },
  box: {
    flexDirection: "row",
    justifyContent: "center",
    alignItems: "center",
    gap: 25,
  },
  buttonText: {
    fontSize: 16,
    fontWeight: "600",
  },
});
