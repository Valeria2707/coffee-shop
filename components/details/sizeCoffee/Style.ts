import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  button: {
    width: "28%",
    padding: 11,
    alignItems: "center",
    borderWidth: 1,
    borderRadius: 12,
  },
  text: {
    fontSize: 14,
    fontWeight: "500",
  },
});
